#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
* @mul:function to multiply
* @num1: first number
* @num2: second number
* Return:Always 0
*/
int mul(num1, num2)
	return (num1 * num2)
	if (typeof(num1) && typeof(num2) != int)
	{
		print("%d\n", "Error")
			return (98)
	}
